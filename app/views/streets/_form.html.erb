<% presenter = StreetPresenter.new %>
<div class="container" style="width: 600px;">
<%= form_with(model: street) do |form| %>
  <% if street.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(street.errors.count, "error") %> prohibited this street from being saved:</h2>

      <ul>
        <% street.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field mb-3">
    <label class="form-label"><%= form.label :name %></label>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="field mb-3">
    <label class="form-label"><%= form.label :district_id %></label>
    <%= form.collection_select :district_id, presenter.ordered_districts, :id, :name %>
  </div>

  <div class="actions mb-2 d-grid gap-2 col-6 mx-auto">
    <%= form.submit 'Save', class:"btn btn-outline-success"  %>
  </div>
<% end %>
</div>
