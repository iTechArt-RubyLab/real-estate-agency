<%= form_with(model: commercial_premises_kind) do |form| %>
  <% if commercial_premises_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commercial_premises_kind.errors.count, "error") %> prohibited this commercial_premises_kind from being saved:</h2>

      <ul>
        <% commercial_premises_kind.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
