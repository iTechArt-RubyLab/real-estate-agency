<%= form_with(model: changes_history) do |form| %>
  <% if changes_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(changes_history.errors.count, "error") %> prohibited this changes_history from being saved:</h2>

      <ul>
        <% changes_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :changes %>
    <%= form.text_field :changes %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
