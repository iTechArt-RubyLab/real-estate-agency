<%= form_with(model: wall_material) do |form| %>
  <% if wall_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wall_material.errors.count, "error") %> prohibited this wall_material from being saved:</h2>

      <ul>
        <% wall_material.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
