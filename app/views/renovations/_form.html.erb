<%= form_with(model: renovation) do |form| %>
  <% if renovation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(renovation.errors.count, "error") %> prohibited this renovation from being saved:</h2>

      <ul>
        <% renovation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
