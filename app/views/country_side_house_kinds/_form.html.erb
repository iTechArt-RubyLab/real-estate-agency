<%= form_with(model: country_side_house_kind) do |form| %>
  <% if country_side_house_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(country_side_house_kind.errors.count, "error") %> prohibited this country_side_house_kind from being saved:</h2>

      <ul>
        <% country_side_house_kind.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
