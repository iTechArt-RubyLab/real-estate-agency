<p id="notice"><%= notice %></p>

<div class="mx-auto" style="width: 200px;">
  <h1>Ready States</h1>
</div>

<table class="table table-striped table-bordered table-hover" style="width:100%" id="ready_states-datatable", data-source="<%= ready_states_path(format: :json) %>">
  <thead>
    <tr style="background-color: #C8EEFF;">
      <th>Name</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>

<script>
  jQuery(document).ready(function() {
  $('#ready_states-datatable').dataTable({
    "processing": true,
    "serverSide": true,
    "ajax": {
      "url": $('#ready_states-datatable').data('source')
    },
    "pagingType": "full_numbers",
    "columns": [
      {"data": "name"},
      {"data": "actions"}
    ]
  });
});
</script>

<%= link_to 'New Ready State', new_ready_state_path, class: "btn btn-outline-info" %>
